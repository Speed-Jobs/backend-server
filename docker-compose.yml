version: '3.8'

services:
  nginx:
    image: nginx:alpine
    container_name: backend-nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    restart: unless-stopped
